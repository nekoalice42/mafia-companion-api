ktor:
  application:
    modules:
      - me.nekoalice.mafia.api.server.AppKt.module
  deployment:
    port: 8080
mafia-api:
  storage:
    # type: pg
    postgresql:
      url: ${DATABASE_URL}
      user: ${DATABASE_USER}
      password: ${DATABASE_PASSWORD}
